node_modules/
*.log
.DS_Store
.env
dist/

# Markdown preview exports (VS Code). WALKTHROUGH.pdf is a deliberate
# deliverable and is tracked; the 34 MB HTML export is not.
/*.html
!/WALKTHROUGH.pdf

# Local copy of the building-mcp-apps skill, for sharing. The skill itself
# lives in ~/.claude/skills/ and is not part of this package.
/building-mcp-apps.zip
